{
  "name": "vite-module-federation",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "preview": "yarn workspaces run preview",
    "stop": "kill-port --port 3000,3001,3002,3003"
  },
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
